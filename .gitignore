node_modules
dist
dist-ssr

# Variables de entorno con credenciales — NUNCA subir a git
.env
.env.local
.env.*.local

*.log
.DS_Store
.vscode/*
!.vscode/extensions.json

# Sesión y caché de whatsapp-web.js (backend) — no subir a git
.wwebjs_auth
.wwebjs_cache

# Herramientas locales de Claude (config de preview, planes, etc.)
.claude
COMMIT_MSG.tmp
