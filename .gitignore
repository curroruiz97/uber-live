node_modules
dist
dist-ssr

# Variables de entorno con credenciales — NUNCA subir a git
.env
.env.local
.env.*.local

*.log
.DS_Store
.vscode/*
!.vscode/extensions.json

# Herramientas locales de Claude (config de preview, planes, etc.)
.claude
COMMIT_MSG.tmp

# IDE
.idea

# Android signing keystores y secretos — NUNCA subir a git
*.keystore
*.jks
android/local.properties
android/key.properties
